DIFERENTES TIPOS DE FAZER UMA FUNÇÃO

- FUNÇÃO BÁSICA ✔
    function somar (){
    let v1 =10;
    let v2 = 10;
    const resultado = v1 + v2;
    console.log(resultado)}

- FUNÇÃO COM RETURN ✔
function somar(v1,v2){
    const resultado = v1 + v2
    return resultado}

- FUNÇÃO COM RETURN v2
function somar(v1, v2){
    return v1 + v2}

CÁLCULO DE PORCENTAGEM

- FUNÇÃO DECLARATIVA
function porcentagem(valor, taxa){
    return valor * (taxa/100)}
const calc = () =>{};

- FUNÇÃO DE SETA
let calcPorcentagem = (valor, taxa) =>{
    return valor * (taxa/100)}

- RESOLUÇÃO 02
let calPorcentagem = (valor, taxa) =>{
    return valor + (valor * (taxa/100))}


Forma literal
let quadrado = {
    lados: 4,
    area: function(lado){return lado * lado},
    perimetro: function(lado){return this.lados * lado}
};

 Forma reduzida - This = contexto do próprio objeto
 const lados = 10;

 const quadrado2 = {
    lados: 4, area(lado){return lado * lado}, perimetro(lado){return this.lados * lado}
};

console.log(quadrado2.perimetro(5))

MÉTODO = Função dentro de um objeto
CLASSE = Maiúscula
VARIÁVEL
